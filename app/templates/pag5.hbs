{{#each model as |student|}}
	<p>{{student.name}}</p>
	<p>{{student.lastName}}</p>
	<p>{{student.curp}}</p>
	{{#link-to 'details' student.id}}
		Editar
	{{/link-to}}
	<button {{action 'deleteStudent' on='click'}}>Delete</button>
{{/each}}
<br><br>
{{input type="text" value=name}}<br>
{{input type="text" value=lastName}}<br>
{{input type="text" value=curp}}<br>
<button {{action 'addStudent' on='click'}}>Create</button>